version: '3.8'

services:
  # 🧠 1. BACKEND SERVICE CONTAINER CONFIGURATION
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - MONGO_URI=mongodb://gigflow_admin:ulALwi4kjh0CFxjt@ac-xpgoxzn-shard-00-00.f28tkur.mongodb.net:27017,ac-xpgoxzn-shard-00-01.f28tkur.mongodb.net:27017,ac-xpgoxzn-shard-00-02.f28tkur.mongodb.net:27017/?ssl=true&replicaSet=atlas-hei3y9-shard-0&authSource=admin&appName=Cluster0
    restart: always

  # 💻 2. FRONTEND SERVICE CONTAINER CONFIGURATION
  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    environment:
      - VITE_API_URL=http://localhost:5000/api
    depends_on:
      - backend
    restart: always